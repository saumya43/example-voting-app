pipeline{
    agent any

    tools{
    nodejs 'NodeJS 8.9.0'
    }

    stages{
        stage('build'){

            steps{
             echo 'compiling result app'
             dir('result'){
                sh 'npm install'
             }
            }

        }
        stage('test'){
          
            steps{
             echo 'running unit tests on result app'
             dir('result'){
             sh 'npm install'
             sh 'npm test'
             }
            }
        }

        }
    }
